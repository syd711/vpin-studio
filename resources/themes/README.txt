You can replace the default theme by replacing the existing .png files with other images of the same size.
You have to copy the files into the actual theme folders (not the "default" folder):

- ./theme/notifications/
- ./theme/pause-menu/